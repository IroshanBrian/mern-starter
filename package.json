{
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "start": "concurrently \"npm run dev --prefix api\" \"npm run dev --prefix frontend\"",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm start --prefix frontend"
  }
}
